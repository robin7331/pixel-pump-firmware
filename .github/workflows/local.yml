# local: the build guard on every push, on whatever branch.
#
# This is the only CI between a commit and a release tag — pixel_pump_main.yml
# fires on `v*` only. It produces nothing: no artifacts, no release, no tag.
# Its whole job is to fail before a bad image can be cut, which is why it runs
# checkFirmwareSize.sh (an oversized image links silently and then overwrites
# the littlefs partition, settings.json included, on first boot) and
# pump_check.py static (the default mapping table drifting from what the
# hardware checks expect).
#
# Replaced pixel_pump_dev.yml, which did the same build and then published a
# draft prerelease on the rolling `latest` tag. The dev branch and the
# prerelease concept both went away on 2026-07-29; releases are now a `v*` tag
# off main, reviewed as a draft, and published by hand. PP2 carries the same
# workflow under the same name.

name: Pixel Pump Local

on:
  push:

jobs:
  local:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - uses: actions/checkout@v4
      with:
        repository: micropython/micropython
        ref: 'v1.28.0'
        path: "./micropython"
    - name: Check the source expectations still hold
      run: python3 $GITHUB_WORKSPACE/tools/pump_check.py static
    - name: Update apt-get
      run: sudo apt-get update
    - name: Install packages
      run: sudo apt-get install gcc-arm-none-eabi libnewlib-arm-none-eabi build-essential cmake git python3 -y
    - name: Generate Version File
      run: |
        python3 $GITHUB_WORKSPACE/tools/generateVersionFile.py --output $GITHUB_WORKSPACE/src/pixel_pump/version.py --repo $GITHUB_WORKSPACE
    - name: Build mpy-cross
      run: |
        cd $GITHUB_WORKSPACE/micropython/
        make -C mpy-cross
    - name: Build ports/rp2 submodules
      run: |
        cd $GITHUB_WORKSPACE/micropython/
        make -C ports/rp2 submodules
    - name: Build Empty Pixel Pump firmware
      run: |
        cd $GITHUB_WORKSPACE/micropython/
        make -C ports/rp2 BOARD_DIR=$GITHUB_WORKSPACE/boards/PIXEL_PUMP BOARD_VARIANT=EMPTY
    - name: Build Pixel Pump Firmware
      run: |
        cd $GITHUB_WORKSPACE/micropython/
        make -C ports/rp2 BOARD_DIR=$GITHUB_WORKSPACE/boards/PIXEL_PUMP clean
        make -C ports/rp2 BOARD_DIR=$GITHUB_WORKSPACE/boards/PIXEL_PUMP
    - name: Check firmware fits below the littlefs partition
      run: |
        $GITHUB_WORKSPACE/tools/checkFirmwareSize.sh \
          $GITHUB_WORKSPACE/micropython/ports/rp2/build-PIXEL_PUMP-EMPTY/firmware.bin \
          $GITHUB_WORKSPACE/micropython/ports/rp2/build-PIXEL_PUMP/firmware.bin
